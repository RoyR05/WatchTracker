export async function trackInteraction(userId: string, mediaId: number, mediaType: string, interactionType: string) {
  // Placeholder for interaction tracking
  console.log('Tracking interaction:', { userId, mediaId, mediaType, interactionType });
}
